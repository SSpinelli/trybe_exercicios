// Para o primeiro exercicio de hoje, faça um programa que, dado um valor n qualquer, seja > 1, imprima um quadrado feito de asteriscos de lado de tamanho n.
let n = 5;
let symbol = "*";
let inputLine = "";

for (let lineIndex = 0; lineIndex < n; lineIndex += 1) {
  inputLine += symbol;
};
for (let lineIndex = 0; lineIndex < n; lineIndex += 1) {
  console.log(inputLine)
};

// para o segundo exercicio de hoje, faça o mesmo que antes, mas que imprima um triângulo retângulo com 5 asteriscos de base;
let m = 5;
let symbol2 = "*";
let inputLine2 = "";

for (let lineIndex = 0; lineIndex <= m; lineIndex += 1) {
  console.log(inputLine2);
  inputLine2 += symbol2;
}

// Agora inverta o lado do triângulo;
let o = 5;
let symbol3 = "*";
let inputLine3 = "";
let inputPosition = o;

for (lineIndex = 0; lineIndex < o; lineIndex += 1) {
  
}


